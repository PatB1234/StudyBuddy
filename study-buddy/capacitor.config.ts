import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.studdybuddy.pratyush',
  appName: 'StuddyBuddy',
  webDir: 'dist/study-buddy/browser'
};

export default config;
